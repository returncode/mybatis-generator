package ${package.Mapper};

import ${package.Entity}.${entity};
#if(${mapperAnnotation})
import org.apache.ibatis.annotations.Mapper;
#end

/**
 * $!{table.comment} Mapper 接口
 *
 * @author ${author}
 * @since ${date}
 */
#if(${mapperAnnotation})
@Mapper
#end
#if(${kotlin})
interface ${table.mapperName} : ${superMapperClass}<${entity}>
#else
public interface ${table.mapperName} {

    /**
     * 根据unid查询$!{table.comment}
     * @param unid $!{table.comment}ID
     * @return $!{table.comment}数据对象
     */
    ${entity} selectByUnid(Long unid);

    /**
     * 新增
     * @param record $!{table.comment}数据对象
     * @return 影响行数
     */
    int insert(${entity} record);

    /**
     * 根据unid更新
     * @param record $!{table.comment}数据对象
     * @return 影响行数
     */
    int updateByUnid(${entity} record);

    /**
     * 根据unid删除
     * @param unid $!{table.comment}ID
     * @return 影响行数
     */
    int deleteByUnid(Long unid);

}
#end